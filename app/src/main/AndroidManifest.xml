<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="webix.example.skalim.contest11">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OnboardingWithCenterAnimationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TabActivity_1"
            android:label="@string/title_activity_contest_main"
            android:theme="@style/AppTheme" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".LandingMainPage"
            android:label="@string/title_activity_landing_main_page" />
        <activity android:name=".FixtureActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Contest11InnerPage" />
        <activity android:name=".CreateTeam" />
        <activity android:name=".CreateTeamMainActivity" />
        <activity android:name=".MainFragment"></activity>
    </application>

</manifest>